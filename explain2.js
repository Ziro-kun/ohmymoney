console.log("If user edits an asset, let's say 전세자금대출 from 50M to 80M, it ONLY edits the asset.amount in the assets table. It does NOT automatically update the `amount` of the transaction '전세대출 이자'. This is what the user is experiencing. The 'calculateDailyBurn' function relies on the Transactions table for fixed expenses. To fix this, I need to implement the next step of the planning document: '5.2 대출 상환액 자동 계산 엔진 (Cascade) 도입'.")
