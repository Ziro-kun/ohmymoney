console.log("The user noticed that the dailyBurnRate / perSecondBurnRate did NOT change when they merely changed the loan *asset* amount from 50,000,000 to 80,000,000. This is because the burn rate is derived from 'fixed transactions' (e.g., the 200,000 KRW Jeonse Interest transaction), NOT directly from the loan principal. To fix this according to user expectations, they would need to either change the transaction amount or we need a real 'automatic calculation engine' for loans that recalculates interest/repayments when loan principal changes. However, right now the engine just aggregates fixed transactions and vehicle depreciation.");
